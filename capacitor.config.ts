import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.donstegall.rcpro',
  appName: 'RCPRO',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
